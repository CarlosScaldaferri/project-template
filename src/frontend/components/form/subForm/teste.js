import React from 'react';
import PropTypes from 'prop-types';

const caduefoda = ({  }) => {
    return (
        <div>
            
        </div>
    );
};

caduefoda.propTypes = {
    
};

// Opcional: Definir defaultProps
// caduefoda.defaultProps = {
// 	
// };

export default caduefoda;